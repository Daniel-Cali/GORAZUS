# tools/database/ — herramientas portables (no versionadas)

`dbeaver/`, `graphviz/`, `schemaspy/` son binarios de terceros (~260MB) — gitignored a propósito,
nunca se commitean. Ver `docs/database/DATABASE_VISUALIZATION.md` para versiones exactas, fuentes
de descarga y cómo reinstalarlas (siempre bajo este directorio, nunca en `C:\`).
