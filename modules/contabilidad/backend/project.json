{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "contabilidad-backend",
  "sourceRoot": "modules/contabilidad/backend",
  "projectType": "library",
  "tags": ["type:backend"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/modules/contabilidad/backend"],
      "options": {
        "command": "tsc -p modules/contabilidad/backend/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/modules/contabilidad/backend"],
      "options": {
        "jestConfig": "modules/contabilidad/backend/jest.config.ts"
      }
    }
  }
}
