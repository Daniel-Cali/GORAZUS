{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-health",
  "sourceRoot": "core/health",
  "projectType": "library",
  "tags": ["type:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/core/health"],
      "options": {
        "command": "tsc -p core/health/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/core/health"],
      "options": {
        "jestConfig": "core/health/jest.config.ts"
      }
    }
  }
}
