{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-kernel",
  "sourceRoot": "core/kernel",
  "projectType": "library",
  "tags": ["type:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/core/kernel"],
      "options": {
        "command": "tsc -p core/kernel/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/core/kernel"],
      "options": {
        "jestConfig": "core/kernel/jest.config.ts"
      }
    }
  }
}
