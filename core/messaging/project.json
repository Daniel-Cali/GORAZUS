{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-messaging",
  "sourceRoot": "core/messaging",
  "projectType": "library",
  "tags": ["type:core"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/core/messaging",
        "main": "core/messaging/index.ts",
        "tsConfig": "core/messaging/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/core/messaging"],
      "options": {
        "jestConfig": "core/messaging/jest.config.ts"
      }
    }
  }
}
