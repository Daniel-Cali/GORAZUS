{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-observability",
  "sourceRoot": "core/observability",
  "projectType": "library",
  "tags": ["type:core"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/core/observability",
        "main": "core/observability/index.ts",
        "tsConfig": "core/observability/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/core/observability"],
      "options": {
        "jestConfig": "core/observability/jest.config.ts"
      }
    }
  }
}
