{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "core-http",
  "sourceRoot": "core/http",
  "projectType": "library",
  "tags": ["type:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/core/http"],
      "options": {
        "command": "tsc -p core/http/tsconfig.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/core/http"],
      "options": {
        "jestConfig": "core/http/jest.config.ts"
      }
    }
  }
}
