# PLANTILLA — placeholders, nunca commitear valores reales acá (este archivo
# SÍ se commitea al repo, por diseño de Kustomize base/). En staging/production
# reales, generar este Secret desde un gestor externo (Sealed Secrets,
# External Secrets Operator contra Vault/AWS Secrets Manager, o `kubectl
# create secret` fuera de git) — ver docs/architecture/31-infraestructura-
# completa.md §2.2. La forma (qué keys existen) sí es parte del contrato
# versionado; los valores, no.
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
type: Opaque
stringData:
  DATABASE_URL: CHANGE_ME
  REDIS_URL: CHANGE_ME
  RABBITMQ_URL: CHANGE_ME
  JWT_ACCESS_SECRET: CHANGE_ME
  JWT_REFRESH_SECRET: CHANGE_ME
  MINIO_ROOT_USER: CHANGE_ME
  MINIO_ROOT_PASSWORD: CHANGE_ME
  NOTIFICATIONS_ENCRYPTION_KEY: CHANGE_ME
